{
  "name": "getir-assignment",
  "version": "1.0.0",
  "description": "This project is a node project which connects to remote mongodb instance and exposes a Post API to fetch data as per request from client and gives response in predefined format.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pankaj Marghade",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0",
    "config": "^3.3.7",
    "express": "^4.17.3",
    "mongoose": "^6.2.4",
    "morgan": "^1.10.0",
    "winston": "^3.6.0"
  }
}
